#include "../h/kthread.h"

int kthread_create(void (*body)(void *), void *arg, void *usr_stack) {
    // later alligator
    return 0;
}

int kthread_exit() {
    // later alligator
    return 0;
}

void kthread_dispatch() {
    // later alligator
}